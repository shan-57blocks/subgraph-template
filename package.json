{
  "name": "erc20-subgraph-example",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "graph clean",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-base-sepolia": "graph deploy --product=subgraph-studio huma-base-sepolia subgraph.yaml",
    "prettier": "prettier --write \"**/*.{js,ts,tsx,graphql,yaml,json}\"",
    "prepare": "husky install"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.1"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "matchstick-as": "0.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
